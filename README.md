A simple to-do list website that stores data in a PostgreSQL database. All you need is Docker and Docker Compose pre-installed. Simply run the following command to set up the entire full-stack application on your local system:

docker-compose up --build
